import { StyleSheet, Text, View } from "react-native";

const ResultSlide = () => {
  return (
    <View>
      <Text>Results go here (Slide6)</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default ResultSlide;
